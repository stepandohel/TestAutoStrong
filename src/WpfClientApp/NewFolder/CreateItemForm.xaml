<Window x:Class="WpfClientApp.NewFolder.CreateItemForm"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfClientApp.NewFolder"
        mc:Ignorable="d"
        Title="CreateItemForm" Height="450" Width="800"
        Name="createItemForm">
    <Grid>
        <Image x:Name="imgPreview" Source="{Binding ElementName=createItemForm, Path=ItemVM.BitmapImage}" HorizontalAlignment="Left" Height="209" Margin="417,31,0,0" VerticalAlignment="Top" Width="210"/>
        <RichTextBox x:Name="richTextBox" Margin="0,0,484,154">
            <FlowDocument>
                <Paragraph>
                    <Run Text="a"/>
                </Paragraph>
            </FlowDocument>
        </RichTextBox>
        <Button Content="Send" HorizontalAlignment="Left" Margin="200,373,0,0" VerticalAlignment="Top" Click="Button_Click_1"/>
        <Button Content="Pick Img" HorizontalAlignment="Left" Margin="487,332,0,0" VerticalAlignment="Top" Click="Button_Click"/>
        <TextBox HorizontalAlignment="Left" Margin="98,324,0,0" TextWrapping="Wrap" Text="{Binding ElementName=createItemForm, Path=ItemVM.Text, Mode=TwoWay}" VerticalAlignment="Top" Width="120"/>

    </Grid>
</Window>
